import { defineConfig } from 'astro/config'

// Note: site will be served at the repo pages root. If you later host
// the site under a subpath such as /TajsGraph set `base: '/TajsGraph'` here.
export default defineConfig({
  // base: '/TajsGraph',
})
